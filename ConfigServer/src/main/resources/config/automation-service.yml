# Automation & Control Service Configuration
server:
  port: 8083

spring:
  application:
    name: automation-service
  datasource:
    url: jdbc:mysql://localhost:3306/AGMS_Db?createDatabaseIfNotExist=true
    username: root
    password: Ijse@1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

# Inter-Service Communication
services:
  zone-service:
    url: http://zone-service/api/zones

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
