# Zone Management Service Configuration
server:
  port: 8081

spring:
  application:
    name: zone-service
  datasource:
    url: jdbc:mysql://localhost:3306/AGMS_Db?createDatabaseIfNotExist=true
    username: root
    password: Ijse@1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

# External IoT API Configuration
iot:
  api:
    base-url: http://104.211.95.241:8080/api

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
