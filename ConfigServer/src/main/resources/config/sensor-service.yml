# Sensor Telemetry Service Configuration
server:
  port: 8082

spring:
  application:
    name: sensor-service
  datasource:
    url: jdbc:mysql://localhost:3306/AGMS_Db?createDatabaseIfNotExist=true
    username: root
    password: Ijse@1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

# External IoT API Configuration
iot:
  api:
    base-url: http://104.211.95.241:8080/api

# Inter-Service Communication
services:
  automation-service:
    url: http://automation-service/api/automation/process

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
